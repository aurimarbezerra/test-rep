[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
commands = pytest --cov=my_project --cov-report=xml --cov-config=tox.ini --cov-branch

[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    tests
 
[coverage:run]